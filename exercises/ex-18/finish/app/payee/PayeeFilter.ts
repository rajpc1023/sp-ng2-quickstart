export class PayeeFilter {
  payeeName: string;
  city: string;
  state: string;

  constructor( payeeName = '', city = '', state = '' ) {
    this.payeeName = payeeName;
    this.city = city;
    this.state = state;
  }

  get
}
